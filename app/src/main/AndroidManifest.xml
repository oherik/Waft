<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alive_n_clickin.commutity" >

    <uses-feature android:name="android.hardware.wifi" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/penguin"
        android:label="@string/app_name"
        android:name="com.alive_n_clickin.commutity.MyApplication">
        <!-- Change the below activity to use waftTheme for style
         (This involves changing waft's theme to App.Compat.Light and make sure the other views still work properly).
         -->
        <activity
            android:name=".presentation.main.MainActivity"
            android:label="@string/app_name"

            android:theme="@style/Theme.AppCompat.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.flagreport.FlagVehicle"
            android:parentActivityName=".presentation.main.MainActivity"
            android:theme="@style/WaftFlagTheme">

        </activity>
    </application>

</manifest>
